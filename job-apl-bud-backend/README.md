User pastes raw CV
↓
extractProfile() → skills[], experience
↓
stored in userProfile (alongside raw CV)
↓
analyzeJob() pulls from userProfile
↓
optimizeCV() uses raw CV + job description → tailored CV
↓
stored in cvVersions
